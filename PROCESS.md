# AI was used to:

-take code from the class repo (fetch.py) to instead, obtain and fetch the data from a different website with prexisting file uploads (the website with all the US extreme weather data)

-suggest imports and different tools to use in plot.py

-implament certain functions (the coordinate-based map plotting stuff) into plot.py

-debug

# Thing kept:

-organizing the stats into a .json (it was great for readability and checking for plot accuracy)

# Thing dropped:

-multiplicative scaling sizes for the dots that persist across months (it overlapped waaaaay too much and got too crazy.)
